<!--================ Start Header Menu Area =================-->
<header class="header_area">
  <div class="main_menu">
    <nav class="navbar navbar-expand-lg navbar-light">
      <div class="container">
        <a class="navbar-brand logo_h" href="/"><img src="img/Arouge-logo-01.png" alt=""></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div class="collapse navbar-collapse offset" id="navbarSupportedContent">
          <ul class="nav navbar-nav menu_nav ml-auto mr-auto">
            <li class="nav-item active"><a class="nav-link" href="/">Home</a></li>
          </ul>

          <ul class="nav-shop">
            <li class="nav-item"><button id="button-shopping-cart"><i class="ti-shopping-cart"></i><span class="nav-shop__circle">3</span></button> </li>
            <li class="nav-item"><a class="button button-header" href="/cart">Buy Now</a></li>
          </ul>
        </div>
      </div>
    </nav>
  </div>
</header>

<style>
    .toaster {
      position: fixed;
      top: 110px;
      right: 20px;
      min-width: 300px;
      min-height: 100px;
      z-index: 250;
      display: flex;
      justify-content: center;
      align-content: stretch;
      margin:0;
      padding:0;
    }
    .toast {
      margin: 1em;
      padding: 1em;
      background: #fafafa;
      min-height: 100px;
      width: 100%;
      display: none;
      transition: all .5s ease;
      position: relative;
      border-radius: 3px;
      color: black;
      box-shadow: 1px 1px 2px rgba(0,0,0,.2);
    }
    .toast.show {
      display: block;
    }
    .toast-close {
      position: absolute;
      top: 0.125em;
      right: 0.125em;
      background: none;
      outline: none;
      border: none;
      cursor: pointer;
      margin:0;
      padding: 0 .5em .25em .25em;
      font-weight: bold;
      color: #aaa;
      user-select: none;
      font-size: 1.25em;
      transition: color .5s ease;
    }
    .toast-close:hover {
      color: #333;
    }
    .toast-title {
      font-weight: bold;
    }
    .toast-body {
      display: flex;
      align-items: center;
      position: relative;
    }
    .toast-body div {
      flex: 1 1 auto;
      margin: 0 0.25em;
    }
    .toast-body :first-child {
      margin-left:0;
    }
    .toast-body :last-child {
      margin-right:0;
    }
    .box-pill {
      border-radius: 1px;
      background: #e6e8f4;
      transition: background .5s ease;
      min-height: 25px;
    }
    .box-pill.active {
      background: #818bc6;
    }
  </style>



<!--================ End Header Menu Area =================-->